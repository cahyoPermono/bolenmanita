/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/bolenmanita',
  assetPrefix: '/bolenmanita',
};

export default nextConfig;
